// This file is no longer used. The server is now started from src/server/index.ts
// with both Next.js and WebSocket server running together.

console.log('Note: This server.js file is no longer used.');
console.log('Please use the following commands instead:');
console.log('  - For development: npm run dev');
console.log('  - For production: npm run build && npm start');

process.exit(1);
